# Project Instructions
- Use the `grep` and `list` tools to understand the codebase.
- Always index the project using local embeddings via Ollama.
- Embedding Model: nomic-embed-text
- Skip any virtual environment such as fevenv or bevenv